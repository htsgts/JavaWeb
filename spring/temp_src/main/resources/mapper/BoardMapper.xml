<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 연결할 인터페이스 정보를 namespace에 기술 -->
<mapper namespace="com.koreait.board.mapper.BoardMapper">
	<sql id="criteria">
		<if test="type != null and keyword != null">
			<trim prefix="AND ">
				<choose>
					<when test="type=='T'.toString()">
						TITLE LIKE CONCAT('%', #{keyword}, '%')
					</when>
					<when test="type=='C'.toString()">
						CONTENT LIKE CONCAT('%', #{keyword}, '%')
					</when>
					<when test="type=='W'.toString()">
						WRITER LIKE CONCAT('%', #{keyword}, '%')
					</when>
				</choose>
			</trim>
		</if>
	</sql>

	<insert id="insert">
		INSERT INTO TBL_BOARD(BNO, TITLE, CONTENT, WRITER)
		VALUES(NEXTVAL(SEQ_BOARD), #{title}, #{content}, #{writer})
	</insert>
	<insert id="insert2">
		INSERT INTO TBL_BOARD(BNO, TITLE, CONTENT, WRITER)
		VALUES(NEXTVAL(SEQ_BOARD), #{title}, #{content}, #{writer})
	</insert>
	<insert id="insertSelectKey">
		<selectKey keyProperty="bno" order="BEFORE" resultType="long">
			SELECT NEXTVAL(SEQ_BOARD) FROM DUAL
		</selectKey>
		INSERT INTO TBL_BOARD(BNO, TITLE, CONTENT, WRITER)
		VALUES(#{bno}, #{title}, #{content}, #{writer})
	</insert>
	<select id="getList" resultType="com.koreait.board.bean.vo.BoardVO">
		<![CDATA[
		SELECT BNO, TITLE, CONTENT, WRITER, REGDATE, UPDATEDATE
		FROM   TBL_BOARD
		WHERE  BNO > 0
		ORDER BY BNO DESC
		]]>
	</select>
	<select id="getListWithKey" resultType="com.koreait.board.bean.vo.BoardVO">
		<![CDATA[
		SELECT BNO, TITLE, CONTENT, WRITER, REGDATE, UPDATEDATE
		FROM   TBL_BOARD
		WHERE  BNO > 0      
		]]>
		<include refid="criteria"></include>		
		ORDER BY BNO DESC
	</select>
	<select id="getListWithPaging" resultType="com.koreait.board.bean.vo.BoardVO">
		<![CDATA[
		SELECT BNO, TITLE, CONTENT, WRITER, REGDATE, UPDATEDATE
		FROM   TBL_BOARD
		WHERE  BNO > 0      
		]]>
		<include refid="criteria"></include>		
		ORDER BY BNO DESC
		LIMIT #{amount} OFFSET #{offset}
	</select>
	<delete id="delete">
		DELETE FROM TBL_BOARD WHERE BNO = #{bno}
	</delete>
	<update id="update">
		UPDATE TBL_BOARD
		SET    TITLE = #{title}
		     , CONTENT = #{content}
		     , WRITER = #{writer}
		     , UPDATEDATE = SYSDATE()
		WHERE  BNO = #{bno}
	</update>
	<select id="get" resultType="com.koreait.board.bean.vo.BoardVO">
		SELECT BNO, TITLE, CONTENT, WRITER, REGDATE, UPDATEDATE
		FROM   TBL_BOARD
		WHERE  BNO = #{bno}
	</select>
	
</mapper>